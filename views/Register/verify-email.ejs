<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Verify</title>

  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f5f7fa;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .container {
      background: white;
      padding: 2rem 3rem;
      border-radius: 8px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 400px;
      box-sizing: border-box;
      text-align: center;
    }
    h2 {
      margin-bottom: 1.5rem;
      color: #333;
    }
    form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    label {
      font-weight: 600;
      color: #555;
      text-align: left;
    }
    input[type="text"] {
      padding: 0.5rem;
      font-size: 1rem;
      border-radius: 4px;
      border: 1px solid #ccc;
      transition: border-color 0.3s ease;
    }
    input[type="text"]:focus {
      border-color: #007bff;
      outline: none;
    }
    button {
      padding: 0.7rem;
      background-color: #007bff;
      color: white;
      font-weight: 600;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    button:hover {
      background-color: #0056b3;
    }
    .message {
      margin-bottom: 1rem;
      font-weight: 600;
    }
    .message.success {
      color: #28a745;
    }
    .message.error {
      color: #dc3545;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2><%= isResetFlow ? 'Reset Password Verification' : 'Verify Your Email' %></h2>

    <% if (message) { %>
      <p class="message success"><%= message %></p>
    <% } %>
    <% if (error) { %>
      <p class="message error"><%= error %></p>
    <% } %>

   <form action="<%= isResetFlow ? '/login/reset-password/verify' : '/login/verify-email' %>" method="POST">
  <input type="hidden" name="email" value="<%= email %>" />
  <% if (isResetFlow) { %>
    <input type="hidden" name="token" value="<%= token %>" />
  <% } %>

  <label for="code">Enter Verification Code:</label>
  <input
    type="text"
    id="code"
    name="code"
    required
    minlength="6"
    maxlength="6"
    pattern="\d{6}"
    placeholder="6-digit code"
    autocomplete="off"
  />
  <button type="submit">Verify</button>
</form>

  </div>
</body>
</html>