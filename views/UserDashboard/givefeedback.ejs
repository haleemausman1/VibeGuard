<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Give Feedback</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link href="img/favicon.ico" rel="icon">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600&family=Roboto:wght@500;700;900&display=swap" rel="stylesheet"> 

    <!-- Icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        body {
            font-family: 'Open Sans', sans-serif;
            background: linear-gradient(to right, #f8fbfd, #eaf4f8);
            margin: 0;
            padding: 0;
        }

        #sidebar {
            min-width: 280px;
            max-width: 280px;
            background: #041D35;
            color: white;
            position: fixed;
            top: 0;
            left: 0;
            height: 100vh;
            padding: 25px 20px;
            display: flex;
            flex-direction: column;
            box-shadow: 4px 0 15px rgba(0, 0, 0, 0.1);
        }

        #sidebar .sidebar-header {
            font-size: 1.8rem;
            font-weight: 800;
            text-align: center;
            padding: 10px 0;
            margin-bottom: 30px;
            border-bottom: 2px solid #063560;
        }

        #sidebar ul.components {
            list-style: none;
            padding: 0;
        }

        #sidebar ul li a {
            color: white;
            padding: 12px 18px;
            display: block;
            border-radius: 10px;
            margin-bottom: 10px;
            font-size: 1rem;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        #sidebar ul li a:hover,
        #sidebar ul li.active > a {
            background-color: #063560;
            color: #ffffff;
            transform: translateX(5px);
        }

        .feedback-wrapper {
            margin-left: 300px;
            padding: 60px 30px;
            max-width: 1000px;
        }

        .feedback-card {
            background: linear-gradient(to top right, #ffffff, #f1f8fb);
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.06);
        }

        .feedback-card h2 {
            font-weight: 800;
            color: #041D35;
            margin-bottom: 25px;
            display: flex;
            align-items: center;
        }

        .feedback-card h2 i {
            margin-right: 10px;
            color: #2684A0;
        }

        .form-label {
            font-weight: 600;
        }

        textarea.form-control {
            border-radius: 12px;
            box-shadow: none;
            border: 1px solid #ccc;
            transition: border-color 0.3s ease;
        }

        textarea.form-control:focus {
            border-color: #2684A0;
            box-shadow: 0 0 0 0.1rem rgba(38, 132, 160, 0.25);
        }

        .btn-primary {
            background-color: #2684A0;
            border: none;
            padding: 10px 25px;
            border-radius: 10px;
            font-weight: 600;
        }

        .btn-primary:hover {
            background-color: #1c6b84;
        }

        .alert-success {
            margin-top: 20px;
            border-radius: 10px;
        }

        /* @media (max-width: 768px) {
            #sidebar {
                position: relative;
                width: 100%;
                height: auto;
            }

            .feedback-wrapper {
                margin-left: 0;
                padding: 30px 15px;
            }
        } */
         @media (max-width: 768px) {
            #sidebar {
                position: fixed;
                width: 240px; /* smaller but still visible */
                height: 100vh;
                left: 0;
                top: 0;
                z-index: 999;
            }

            .feedback-wrapper {
                margin-left: 260px; /* match sidebar width */
                padding: 20px;
            }
        }

        /* @media (max-width: 560px) {
            #sidebar {
                width: 180px;  
                min-width: 180px;
                max-width: 180px;
            }

            .feedback-wrapper {
                margin-left: 200px; 
                padding: 20px;
            }
        } */

        /* Between 426px and 560px */
@media (max-width: 560px) {
    #sidebar {
        width: 180px;
        min-width: 180px;
        max-width: 180px;
    }

    .feedback-wrapper {
        margin-left: 200px; /* matches sidebar */
        padding: 20px;
    }
}

@media (max-width: 425px) {
    #sidebar {
        width: 120px;
        min-width: 120px;
        max-width: 120px;
        font-size: 0.8rem;
    }

    #sidebar .sidebar-header {
        font-size: 1rem;      /* smaller text */
        line-height: 1.2;     /* tighter line height */
        text-align: left;     /* align left */
        padding-left: 5px;    /* slight left shift */
    }

    #sidebar ul li a {
        padding: 6px 8px;
        font-size: 0.75rem;
    }

    .feedback-wrapper {
        margin-left: 130px;
        padding: 10px;
    }

    .feedback-card {
        padding: 18px;
    }

    .feedback-card h2 {
        font-size: 0.95rem;
        flex-wrap: wrap;
    }

    .feedback-card h2 i {
        font-size: 0.95rem;
    }

    textarea.form-control {
        font-size: 0.8rem;
    }

    .btn-primary {
        width: 100%;
        font-size: 0.85rem;
    }
}

/* Default: hide back button */
.back-btn {
    position: fixed;
    top: 15px;
    left: 15px;
    z-index: 1000;
    display: none;
}

/* Small screens: hide sidebar, show back button */
@media (max-width: 768px) {
    #sidebar {
        display: none;
    }

    .feedback-wrapper {
        margin-left: 0;
        padding: 70px 20px 20px; /* ⬅ added top padding to clear back button */
    }

    .back-btn {
        display: inline-block;
    }
}



    </style>
</head>
<body>

    <nav id="sidebar">
        <div class="sidebar-header">User Dashboard</div>
        <ul class="components">
            <li class="active">
                <a href="/tracking/dashboard"><i class="bi bi-arrow-left me-2"></i>Back</a>
            </li>
        </ul>
    </nav>

    <a href="/tracking/dashboard" class="btn btn-sm btn-dark d-block d-md-none back-btn">
        <i class="bi bi-arrow-left"></i> Back To Dashboard
    </a>

    <div class="feedback-wrapper">
        <div class="feedback-card">
            <h2><i class="bi bi-chat-left-dots"></i> Give Your Feedback</h2>

            <% if (success) { %>
                <div class="alert alert-success" role="alert">
                    Feedback submitted successfully!
                </div>
            <% } %>

            <form action="/feedback/create" method="POST">
                
                <div class="mb-3">
                    <label for="feedback" class="form-label">Your Feedback</label>
                    <textarea class="form-control" id="feedback" name="feedbackText" rows="4" required placeholder="We value your thoughts..."></textarea>
                </div>
                <button type="submit" class="btn btn-primary"><i class="bi bi-send me-1"></i> Submit Feedback</button>
            </form>

        </div>
    </div>

</body>
</html>
